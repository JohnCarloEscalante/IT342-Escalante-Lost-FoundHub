class Item {
  constructor(id, title, description, category, location, imageUrl, status) {
    this.id = id
    this.title = title
    this.description = description
    this.category = category
    this.location = location
    this.imageUrl = imageUrl
    this.status = status
  }
}

export default Item